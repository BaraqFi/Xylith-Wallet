# ── Site URLs ───────────────────────────────────────────────────────────────
# This app's origin is unchanged by the landing-page split.
NEXT_PUBLIC_APP_URL=https://xylith-wallet.vercel.app

# Wherever the landing page ends up — used by the "What is Xylith?" link.
NEXT_PUBLIC_MARKETING_URL=https://xylith.vercel.app

# ── Secrets (see .env.local for the full set) ───────────────────────────────
# NEXT_PUBLIC_PRIVY_APP_ID=
# PRIVY_APP_SECRET=
# AI_SESSION_KEY_SECRET=   # required — server refuses to boot without it
